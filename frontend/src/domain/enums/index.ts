// Domain Enums
export enum UserRole {
  ADMIN = 'admin',
  STAFF = 'staff',
  CUSTOMER = 'customer',
}

export enum TableStatus {
  AVAILABLE = 'available',
  PLAYING = 'playing',
  MAINTENANCE = 'maintenance',
}

export enum SessionStatus {
  PLAYING = 'playing',
  COMPLETED = 'completed',
  CANCELLED = 'cancelled',
}